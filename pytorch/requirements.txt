# Runtime dependencies for lstm-nlp.
# Install torch from the CPU index to avoid pulling CUDA wheels:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
#
# Already present on the dev machine (2026-08-29): numpy 2.2.6, pandas 2.3.3,
# scikit-learn 1.7.2, pydantic 2.13.4, PyYAML 6.0.3, fastapi 0.141.1,
# uvicorn 0.52.3, pytest 9.1.1.  Only torch and tqdm needed installing.

torch>=2.2,<3
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
pydantic>=2.5
PyYAML>=6.0
tqdm>=4.66
fastapi>=0.110
uvicorn>=0.27

# frontend (Rules.md section 2). Declared here rather than left transitive:
# frontend/charts.py imports altair directly, and a direct import that relies
# on someone else's dependency tree breaks the day they drop it.
streamlit>=1.40,<2
altair>=6.0,<7
httpx>=0.27,<1        # the frontend's HTTP client, and starlette's TestClient transport

# dev
pytest>=8.0
ruff>=0.4
